import { LayoutData } from "@lichtblick/suite-base/context/CurrentLayoutContext/actions";
export declare function replacePanel(panelsState: LayoutData, oldId: string, newId: string, newConfig: Record<string, unknown>): LayoutData;
