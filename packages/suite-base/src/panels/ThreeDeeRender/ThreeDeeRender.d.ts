import React from "react";
import { ThreeDeeRenderProps } from "./types";
/**
 * A panel that renders a 3D scene. This is a thin wrapper around a `Renderer` instance.
 */
export declare function ThreeDeeRender(props: Readonly<ThreeDeeRenderProps>): React.JSX.Element;
