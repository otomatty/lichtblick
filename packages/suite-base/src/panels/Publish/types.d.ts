export type PublishConfig = {
    topicName?: string;
    datatype?: string;
    buttonText?: string;
    buttonTooltip?: string;
    buttonColor?: string;
    advancedView: boolean;
    value?: string;
};
