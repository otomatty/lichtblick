/// <reference types="react" />
/**
 * Renders the given text with the span matching highlight wrapped in a
 * <mark> component.
 */
export declare function HighlightedText({ text, highlight, }: {
    text: string;
    highlight?: string;
}): React.JSX.Element;
